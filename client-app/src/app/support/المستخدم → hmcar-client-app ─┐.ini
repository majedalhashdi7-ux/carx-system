المستخدم → hmcar-client-app ─┐
                               ├→ hmcar-system (API + DB)
المستخدم → carx-system    ────┘

المستخدم → client-app (port أو Vercel) ─────┐
                                              ├──→ hmcar-system API (Express)
المستخدم → carx-system (netlify.app)  ───────┤         │
              │                               │    MongoDB (DB)
              └── /api/* (Next.js routes) ───┘
                     تتصل بنفس MongoDB مباشرة

  c:\car-auction\               ← hmcar-system (الخادم الرئيسي)
├── server.js                   Express API
├── routes/                     30+ مسار API
├── models/                     39 نموذج MongoDB
├── middleware/
├── services/
└── ...

c:\car-auction\client-app\    ← hmcar-client-app (الواجهة القديمة)
└── Next.js frontend

c:\car-auction\carx-system\   ← carx-system (الواجهة الجديدة)
└── Next.js frontend + /api routes خاصة به             